# Reference Implementation (Standards Only)

This folder intentionally contains **standards/specs only** (e.g., tool contract standard + schemas) to keep the architecture repository vendor-neutral.

✅ **Runnable sample code** and any executable reference implementation lives in the companion repo:
- `ai-reference-implementation/reference-implementation/`

Contents here:
- `standards/` — tool contract standard and schemas

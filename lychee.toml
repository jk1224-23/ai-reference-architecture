# Lychee link checker configuration
# GitHub links may return 404 if repos are private or rate-limited

exclude = [
  "http://localhost.*",
  "http://127\\.0\\.0\\.1.*",
]

# Accept 404s (some GitHub repos may be private)
accept = [404]
